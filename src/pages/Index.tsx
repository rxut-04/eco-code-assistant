
import React from 'react';
import { Layout } from '@/components/Layout';

const Index = () => {
  return (
    <Layout>
      {/* Main content will be rendered from the Layout component */}
    </Layout>
  );
};

export default Index;
